table:
  schema: auth
  name: completed_quizes
insert_permissions:
  - role: public
    permission:
      check: {}
      columns:
        - created_at
        - id
        - quiz_id
        - updated_at
        - user_id
      backend_only: false
