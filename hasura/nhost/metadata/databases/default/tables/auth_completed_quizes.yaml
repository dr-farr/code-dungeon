table:
  name: completed_quizes
  schema: auth
insert_permissions:
- permission:
    backend_only: false
    check: {}
    columns:
    - created_at
    - id
    - quiz_id
    - updated_at
    - user_id
  role: public
