table:
  name: quizes
  schema: auth
array_relationships:
- name: questions
  using:
    foreign_key_constraint_on:
      column: quiz_id
      table:
        name: questions
        schema: auth
select_permissions:
- permission:
    allow_aggregations: true
    columns:
    - created_at
    - description
    - difficulty
    - id
    - name
    filter: {}
  role: public
