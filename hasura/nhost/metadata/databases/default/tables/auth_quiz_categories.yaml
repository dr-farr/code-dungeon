table:
  name: quiz_categories
  schema: auth
select_permissions:
- permission:
    allow_aggregations: true
    columns:
    - description
    - id
    - image_url
    - name
    filter: {}
  role: user
